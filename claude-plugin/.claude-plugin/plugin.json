{
  "name": "overdrive-intel",
  "description": "Live intelligence feed for the AI coding ecosystem. Monitors 550+ sources daily for updates on MCP servers, Claude Code, LLM APIs, agent frameworks, and AI coding tools. Get breaking changes, best practices, and curated briefings injected directly into your sessions.",
  "version": "1.0.0",
  "mcpServers": {
    "overdrive-intel": {
      "command": "npx",
      "args": ["-y", "overdrive-intel-mcp@latest"],
      "env": {
        "OVERDRIVE_API_URL": "${OVERDRIVE_INTEL_API_URL:-https://inteloverdrive.com}",
        "OVERDRIVE_API_KEY": "${OVERDRIVE_INTEL_API_KEY}"
      }
    }
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ./hooks/session-start.sh"
          }
        ]
      }
    ]
  }
}
